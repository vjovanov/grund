Renumbered on import: §FS-042→FS-043, and §FS-042/043 both moved.

Only the marked head is a citation at all — FS-043 here is a bare number, which
is why its declaration is still reported uncited below.

The escape the message names clears it: <§>FS-042→FS-043

An ordinary citation is untouched by the rule: §FS-042

A construct boundary is not a delimiter run, so the link this project writes and
a footnote reference are ordinary shorthand citations that `fmt` will expand:
[§FS-042](FS-042-user-login.md) and §FS-042[^1]

[^1]: The reference above, not a second number.

appended AGENTS.md
appended AGENTS.override.md
appended CLAUDE.md
appended .claude/CLAUDE.md
appended GEMINI.md
appended .github/copilot-instructions.md
appended .cursor/rules/grund.mdc
appended .cursorrules
appended .windsurfrules
appended .rules
wrote grund.toml
note: CLAUDE.md and .claude/CLAUDE.md both carry the managed block, so Claude reads it twice; delete the one you do not want — `grund init` creates only one
note: .cursor/rules/grund.mdc and .cursorrules both carry the managed block, so Cursor reads it twice; delete the one you do not want — `grund init` creates only one

next:
  1. re-run with --docs to scaffold the FS home (requirements.md), docs/, and e2e/ (or create them yourself) — until then `grund check` has nothing to scan
  2. run `grund check` — a scaffolded tree is clean
  3. allocate an ID:  ID=$(grund id FS "…")  then add it to requirements.md
see AGENTS.md for the full workflow.

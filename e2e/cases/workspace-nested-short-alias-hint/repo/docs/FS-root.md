# FS-root: Root

The root writes the short leaf names §alpha/FS-alpha and §beta/FS-beta where the
whole alias path is required, and the diagnostic names the project each one
could have meant. It cites §group/FS-group correctly.

"""Simulation world.

Cites §FS-001-login in a module docstring that runs well past the
three-line inline-note budget, because this directory was never in
`[scan] include` and never wrote to that convention.
"""

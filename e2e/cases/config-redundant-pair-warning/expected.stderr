warning: .agents/grund.toml is ignored — grund.toml takes precedence; delete one
warning: nothing recognized — grund read 1 file and found no declaration and no citation in them. A declaration heading reads `# <KIND>-<NNN>-<slug>: <title>` and a citation `§<KIND>-<NNN>-<slug>`, under [id] format = "{kind}-{number}-{slug}" with <KIND> one of {GRUND, GOAL, FS, AR, DF, DA, E2E, RM}. Check [id] format and [[kinds]] in grund.toml against how the headings are written.

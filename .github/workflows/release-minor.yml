# Manual minor releases implement §FS-distribution.4.
name: Release minor

# Manual minor release helper: from the latest vX.Y.Z tag, bump to vX.(Y+1).0,
# commit the workspace version update to main, and dispatch release.yml to
# publish crates + binaries. release.yml remains the publishing workflow; this
# file only prepares the version bump.

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: write   # gh workflow run needs actions:write to dispatch

env:
  CARGO_TERM_COLOR: always

jobs:
  minor-release:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          # Use the same release PAT as auto-bump.yml so the version commit can
          # pass the main-branch ruleset bypass configured for releases.
          token: ${{ secrets.RELEASE_PAT }}

      - name: Resolve current and next versions
        id: versions
        shell: bash
        run: |
          set -euo pipefail
          tag="$(git tag --list 'v*.*.*' --sort=-v:refname | head -n1)"
          if [ -z "$tag" ]; then
            echo "error: no v*.*.* tag exists; bootstrap the first release manually" >&2
            exit 1
          fi
          current="${tag#v}"
          IFS=. read -r major minor patch <<< "$current"
          next="${major}.$((minor + 1)).0"
          echo "current=$current"  >> "$GITHUB_OUTPUT"
          echo "next=$next"        >> "$GITHUB_OUTPUT"
          echo "current_tag=$tag"  >> "$GITHUB_OUTPUT"
          echo "next_tag=v$next"   >> "$GITHUB_OUTPUT"
          echo "Latest tag: $tag; proposed minor release: v$next"

      - name: Gate - commits since last tag
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ steps.versions.outputs.current_tag }}"
          count="$(git rev-list --count "$tag"..HEAD)"
          if [ "$count" -eq 0 ]; then
            echo "error: no commits since $tag; refusing to create an empty minor release" >&2
            exit 1
          fi
          echo "$count commits since $tag."

      - name: Gate - CI on current main tip is green
        shell: bash
        run: |
          set -euo pipefail
          sha="$(git rev-parse HEAD)"
          # The CI workflow runs on push to main. Find the most recent run that
          # targeted this exact tip; if none exists yet, the bump must wait.
          run="$(gh run list --workflow=CI --branch=main --limit=20 \
            --json status,conclusion,headSha,databaseId \
            | jq -c ".[] | select(.headSha == \"$sha\")" | head -n1)"

          if [ -z "$run" ]; then
            echo "error: no CI run found for HEAD ($sha); rerun once CI has finished" >&2
            exit 1
          fi
          status="$(echo "$run" | jq -r '.status')"
          conclusion="$(echo "$run" | jq -r '.conclusion')"
          if [ "$status" != "completed" ] || [ "$conclusion" != "success" ]; then
            echo "error: CI on $sha is $status/$conclusion (need completed/success)" >&2
            exit 1
          fi
          echo "CI on $sha is green."

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-edit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-edit

      - name: Bump workspace version and refresh lockfile
        shell: bash
        run: |
          set -euo pipefail
          cargo set-version --workspace "${{ steps.versions.outputs.next }}"
          cargo update --workspace
          printf 'grund %s\n' "${{ steps.versions.outputs.next }}" > tests/e2e/cases/cli-version/expected.stdout
          python3 scripts/prepare_changelog_release.py prepare "${{ steps.versions.outputs.next }}"

      - name: Commit and push release candidate
        id: candidate
        shell: bash
        run: |
          set -euo pipefail
          next="${{ steps.versions.outputs.next }}"
          branch="release-candidate/v${next}-${GITHUB_RUN_ID}"
          base_sha="$(git rev-parse HEAD)"

          git switch -c "$branch"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'

          git add Cargo.toml crates/grund-core/Cargo.toml crates/grund-cli/Cargo.toml crates/grund-lsp/Cargo.toml Cargo.lock tests/e2e/cases/cli-version/expected.stdout docs/changelog.md docs/changelog
          git commit -m "Release v${next}"
          git push -u origin "$branch"

          sha="$(git rev-parse HEAD)"
          echo "branch=$branch" >> "$GITHUB_OUTPUT"
          echo "base_sha=$base_sha" >> "$GITHUB_OUTPUT"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

      - name: Dry-run release workflow
        shell: bash
        run: |
          set -euo pipefail
          next="${{ steps.versions.outputs.next }}"
          branch="${{ steps.candidate.outputs.branch }}"
          sha="${{ steps.candidate.outputs.sha }}"

          gh workflow run release.yml \
            --ref "$branch" \
            -f version="$next" \
            -f publish_crates=false \
            -f create_github_release=false

          run_id=""
          for _ in $(seq 1 60); do
            run_id="$(gh run list --workflow=release.yml --branch="$branch" --event=workflow_dispatch --limit=20 \
              --json databaseId,headSha \
              | jq -r ".[] | select(.headSha == \"$sha\") | .databaseId" \
              | head -n1)"
            if [ -n "$run_id" ]; then
              break
            fi
            sleep 5
          done

          if [ -z "$run_id" ]; then
            echo "error: dispatched release dry run for $branch but could not find the run" >&2
            exit 1
          fi

          gh run watch "$run_id" --exit-status --interval 30

      - name: Publish release commit and dispatch release
        shell: bash
        run: |
          set -euo pipefail
          next="${{ steps.versions.outputs.next }}"
          branch="${{ steps.candidate.outputs.branch }}"
          base_sha="${{ steps.candidate.outputs.base_sha }}"
          sha="${{ steps.candidate.outputs.sha }}"

          git fetch origin main
          current_main="$(git rev-parse refs/remotes/origin/main)"
          if [ "$current_main" != "$base_sha" ]; then
            echo "error: main moved from $base_sha to $current_main while the release dry run was running" >&2
            echo "       leaving $branch in place; rerun the minor release from the new main tip" >&2
            exit 1
          fi

          git push origin "${sha}:refs/heads/main"

          # GITHUB_TOKEN pushes do not fan out to other workflows, so dispatch
          # release.yml directly from main after the verified version bump lands.
          gh workflow run release.yml \
            --ref main \
            -f version="$next" \
            -f publish_crates=true \
            -f create_github_release=true
          git push origin --delete "$branch" || true

      - name: Advance main to the next dev version
        shell: bash
        run: |
          set -euo pipefail
          released="${{ steps.versions.outputs.next }}"
          IFS=. read -r major minor patch <<< "$released"
          dev="${major}.${minor}.$((patch + 1))-dev"
          # Without this main keeps the version it just released until the next
          # bump, so a binary built from main is indistinguishable from the tag it
          # is ahead of. The suffix is what makes `--version` say which side of
          # the tag a build came from.
          git fetch origin main
          git checkout -B main refs/remotes/origin/main
          cargo set-version --workspace "$dev"
          # The version fixture is checked, so a bump that skips it pushes a main
          # whose own e2e suite fails. The release bump writes the same file.
          printf 'grund %s\n' "$dev" > tests/e2e/cases/cli-version/expected.stdout
          git add -A
          git commit -m "Open ${dev} for development"
          git push origin main
          echo "Dispatched release.yml for v$next."

# Into the member: `vendored` owns those files, so the root project's narrowed
# walk stops here and `--write` never reaches them.
docs/member.md -> ../vendored/docs/FS-002-beta.md
# Onto a file no project owns: file links remain followed, so this one is walked
# and rewritten — the canonical-root fence applies to directory traversal.
docs/loose.md -> ../outside/notes.md

grund_config_version = 1
project_name = "acme"

[reference]
marker = "§"
strict = true

[id]
format = "{kind}-{slug}"

[[kinds]]
prefix = "SPEC"
file = "docs/spec.md"
title = "What the service does"

[scan]
include = ["docs"]

[workspace]
members = ["vendored"]

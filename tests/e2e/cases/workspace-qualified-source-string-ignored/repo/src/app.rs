/// Ties into the API session behavior in §api/FS-session.
fn wire() {
    let example = "see §api/FS-session"; // suppressed: string literal
    let _ = example;
}
/// Example syntax: `§api/FS-session` (suppressed: inline code in source).
fn doc() {}

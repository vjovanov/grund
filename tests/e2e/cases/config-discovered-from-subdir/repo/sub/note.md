# Note

Mentions FS-001-foo without a marker. The repo config two directories up
sets `strict = true`, so this bare token is not a citation and `check`
must stay silent — proving config discovery walked up from this subdir.

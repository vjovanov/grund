# `--write` turns `--cross-refs` on by itself here, so the whole declaration set
# is needed before anything is rewritten and every broken path is fatal up front.
docs/functional-spec/FS-002-gone.md -> FS-002-gone-target.md
docs/functional-spec/FS-003-also-gone.md -> FS-003-also-gone-target.md

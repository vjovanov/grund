# FS-alpha: The alpha spec

Written slug-only, under an `[id] format` of `{kind}-{number}-{slug}`. The
heading looks like a declaration and is not one, so the tree declares nothing,
cites nothing, and used to check clean.

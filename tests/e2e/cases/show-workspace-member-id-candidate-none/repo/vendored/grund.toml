grund_config_version = 1
project_name = "vendored"

[reference]
marker = "§"
strict = true

[id]
format = "{kind}-{number}-{slug}"

[[kinds]]
prefix = "SPEC"
file = "docs/spec.md"
title = "What the service does"

[scan]
include = ["docs"]

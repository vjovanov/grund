// This note cites §FS-fixture and deliberately spans four lines.
// Its error sorts after the warning path without severity grouping.
// The third line keeps this one contiguous inline note.
// The fourth line exceeds the configured maximum.
pub fn fixture() {}

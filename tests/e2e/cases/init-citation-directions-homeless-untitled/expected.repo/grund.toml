grund_config_version = 1
project_name = "demo"

# The homeless kind without a `title` (§FS-config.3.9.2): its bullet is the
# fixed subject alone.

[id]
format = "{kind}-{slug}"

[[kinds]]
kind = "FS"
folder = "docs/functional-spec"
index = false

[[kinds]]
kind = "AR"
folder = "docs/architecture"
index = false

[citations]

[citations.FS]
should = ["AR"]

[citations.code]
should = ["FS|AR"]

# Offer

Exact decimal spellings agree: `1200.0` (§CONST-field-price.1).

This adjacent literal is deliberately not bound: 1200 (§CONST-field-price.1).

The unit is an exact string: `USD` (§CONST-field-price.2).

The intentionally stale discount is `0.30` (§CONST-discount.1).
